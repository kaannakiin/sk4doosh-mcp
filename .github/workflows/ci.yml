name: CI

on:
  workflow_dispatch:
  workflow_call:

# github.workflow is the caller's name under workflow_call, so a release
# build started by publish.yml never shares a group with a push to main,
# which would cancel it halfway through.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  native:
    name: secure files (${{ matrix.target.id }}, Node ${{ matrix.node }})
    runs-on: ${{ matrix.target.os }}
    strategy:
      fail-fast: false
      matrix:
        # Only Node 24 uploads a prebuild, so the Node 22 legs are pure
        # compatibility checks and double the bill for no artifact. They run on
        # a dispatched round — which is what a native release is — or when a
        # commit asks for them with [node22].
        node: [22, 24]
        # pdf: whether the PDF engine publishes a prebuilt binary for this
        # target. @firecrawl/pdf-inspector ships no darwin-x64 build and its
        # wasm fallback is unpublished, so @liaiso/pdf-mcp is left out of that
        # leg rather than failing it.
        target:
          - { id: linux-x64, os: ubuntu-24.04, pdf: true }
          - { id: linux-arm64, os: ubuntu-24.04-arm, pdf: true }
          - { id: darwin-x64, os: macos-15-intel, pdf: false }
          - { id: darwin-arm64, os: macos-15, pdf: true }
          - { id: win32-x64, os: windows-2022, pdf: true }
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.23.0
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - run: pnpm install --frozen-lockfile
      - run: node .github/scripts/prepare-native.mjs
      - run: pnpm turbo run test check-types --filter=@liaiso/file-core-native --filter=@liaiso/mcp-core --filter=@liaiso/db-core --filter=@liaiso/mssql-mcp --filter=@liaiso/file-core --filter=@liaiso/ooxml-core --filter=@liaiso/excel-mcp --filter=@liaiso/xml-mcp --filter=@liaiso/llm-mcp ${{ matrix.target.pdf && '--filter=@liaiso/pdf-mcp --filter=@liaiso/pdf-raster-pdfjs --filter=@liaiso/ocr-ollama' || '' }}
      - run: pnpm pack --filter=@liaiso/file-core-native --filter=@liaiso/mcp-core --filter=@liaiso/db-core --filter=@liaiso/mssql-mcp --filter=@liaiso/file-core --filter=@liaiso/ooxml-core --filter=@liaiso/excel-mcp --filter=@liaiso/xml-mcp --filter=@liaiso/llm-mcp ${{ matrix.target.pdf && '--filter=@liaiso/pdf-mcp --filter=@liaiso/pdf-raster-pdfjs --filter=@liaiso/ocr-ollama' || '' }} --pack-destination ./local/native-tarballs
      - run: python3 .github/scripts/check-npm-tarballs.py local/native-tarballs
      - run: node .github/scripts/smoke-file-packages.mjs local/native-tarballs
      - uses: actions/upload-artifact@v7
        if: matrix.node == 24
        with:
          name: native-${{ matrix.target.id }}
          path: packages/cores/file-core-native/prebuilds/
          if-no-files-found: error

  node:
    name: build, lint, types, fixtures
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.23.0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: |
            8.0.x
            10.0.x
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo run build check-types lint validate --filter='!@chat/*'
      - run: pnpm turbo run format:check
      - run: pnpm turbo run test --filter='!@liaiso/sdk-dotnet' --filter='!@chat/*'

  dotnet:
    name: dotnet test (net8.0 + net10.0)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.23.0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: |
            8.0.x
            10.0.x
      - run: pnpm install --frozen-lockfile
      - run: node packages/http/conformance/validate.mjs
      - run: pnpm turbo run test --filter=@liaiso/sdk-dotnet

  pack:
    name: pack Liaiso.AspNetCore
    runs-on: ubuntu-latest
    needs: [node, dotnet, native]
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v6
        with:
          version: 11.23.0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: |
            8.0.x
            10.0.x
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo run pack --filter=@liaiso/sdk-dotnet
      - uses: actions/upload-artifact@v7
        with:
          name: Liaiso.AspNetCore-nupkg
          path: sdks/dotnet/local/nupkg-feed/*
      - run: pnpm turbo run build --filter=@liaiso/mcp-core --filter=@liaiso/db-core --filter=@liaiso/mssql-mcp --filter=@liaiso/file-core --filter=@liaiso/ooxml-core --filter=@liaiso/excel-mcp --filter=@liaiso/xml-mcp --filter=@liaiso/pdf-mcp --filter=@liaiso/pdf-raster-pdfjs --filter=@liaiso/ocr-ollama --filter=@liaiso/llm-mcp
      - uses: actions/download-artifact@v8
        with:
          pattern: native-*
          path: packages/cores/file-core-native/prebuilds/
          merge-multiple: true
      - run: pnpm pack --filter=@liaiso/file-core-native --filter=@liaiso/mcp-core --filter=@liaiso/db-core --filter=@liaiso/mssql-mcp --filter=@liaiso/file-core --filter=@liaiso/ooxml-core --filter=@liaiso/excel-mcp --filter=@liaiso/xml-mcp --filter=@liaiso/pdf-mcp --filter=@liaiso/llm-mcp --filter=@liaiso/pdf-raster-pdfjs --filter=@liaiso/ocr-ollama --pack-destination ./local/npm-tarballs
      - run: python3 .github/scripts/check-npm-tarballs.py local/npm-tarballs
        env:
          LIAISO_REQUIRE_ALL_PREBUILDS: "1"
      - run: node .github/scripts/smoke-file-packages.mjs local/npm-tarballs
      - uses: actions/upload-artifact@v7
        with:
          name: npm-tarballs
          path: local/npm-tarballs/*.tgz
