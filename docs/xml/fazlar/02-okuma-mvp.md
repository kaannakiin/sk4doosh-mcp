# F2 — Salt okunur XML MVP

Durum: **tamamlandı** (2026-09-09). F2-01/02/03 F1 kapanışında, F2-04–12 bu teslimde kapandı; platform kanıtı CI 34349535283 (13/13) ve XML F0 34349535299 (10/10), commit `a088abd`. Ayrıntı [F2 kapanış kaydında](../xml-f2-kapanis.md). Sorumlu: XML geliştiricisi; doğrulayan: entegrasyon/test inceleyicisi. Önkoşul karşılandı: F0 geçti, F1'in XML kapıları uygulandı.

## Hazır altyapı ve kalan entegrasyon

F1-01/02/04/07'nin ortak dosya erişimi, snapshot/cache ve listeleme uygulaması platform CI'ında geçti. F2 bu altyapıyı tüketiyor: parser girdisi `ParseContext.bytes`, içerik kimliği `stamp`, kullanıcıya gösterilen yol `displayPath`; `path` parser tarafından yeniden açılmıyor. `packages/xml-mcp`, XML format registry'si, tek parse policy'si, worker yürütücüsü ve `list_documents` F2-01/02/03 ile teslim edilmişti. Genişletilmiş düğüm modeli, kalan üç tool, sayfa bütçesi, bellek katsayısı ve kalan encoding aileleri F2-04–12 ile tamamlandı. Yayınlanan yüzeyi değiştiren kararlar [karar 017](../../kararlar/017-xml-dugum-modeli-ve-yanit-sayfasi.md)'dedir.

## Teslim

Dört tool: `list_documents`, `describe_document`, `read_node`, `find_in_document`. Girdi/çıktı davranışı [tool sözleşmesinde](../tool-sozlesmesi.md). İlk yayın F2 sonrasında F6 kapısından geçebilir; XPath zorunlu değildir.

| Görev | İş                                                                              | Somut çıktı                                                    | Kabul ölçütü                                                                                                                                                                                                                                                                                                                                                                                                             |
| ----- | ------------------------------------------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F2-01 | Tamamlandı: yayınlanabilir paket ve bağlantı yüzeyi                             | `packages/xml-mcp` ESM/stdio/exports/bin ve file-core adaptörü | `core` bağımlılığı yok; publishable paket private workspace paketine bağlı değil — `pack` job'ı ve tarball denetleyicisi doğruluyor; read-only anotasyonları gerçek MCP istemcisiyle sınandı                                                                                                                                                                                                                             |
| F2-02 | Tamamlandı: tek parse policy ve worker yürütücüsü                               | Byte girdi, F0 limitleri, normalize hata, iptal/cleanup yolu   | Parser kullanıcının verdiği bayrakları kabul etmiyor; sertleştirilmiş bayraklar sabit; DOCTYPE parse'tan önce reddediliyor; invalid XML başarıya dönüşmüyor. `unsupported_encoding` ayrımı motor-mesajı eşlemesi gerektirdiği için bu teslimde yok                                                                                                                                                                       |
| F2-03 | Tamamlandı: XML format registry ve dosya listeleme                              | Allowlist + `list_documents`                                   | XML uzantılı dizin ve kök dışı symlink reddediliyor; scan bütçesi ve eksik toplam işaretli; listeleme parse yapmıyor                                                                                                                                                                                                                                                                                                     |
| F2-04 | **Tamamlandı:** genişletilmiş ad, adres ve sıralı node modeli                   | XML'e özgü namespace/address katmanı                           | Default namespace, prefix rebinding ve aynı local name/farklı URI doğru ayrışıyor; whitespace/mixed content sırası korunuyor                                                                                                                                                                                                                                                                                             |
| F2-05 | **Tamamlandı:** yapı keşfi                                                      | `describe_document`                                            | Root, namespace alias'ları, örnek kullanılabilir adres, yetenek/sınır ve kısmi yapı bilgisi var; sezgi şema garantisi sayılmıyor                                                                                                                                                                                                                                                                                         |
| F2-06 | **Tamamlandı:** bütçeli düğüm okuma                                             | `read_node` ve devam sözleşmesi                                | Derinlik/node/byte sınırı birlikte uygulanıyor; cursor ilerliyor, tek büyük text açık kesiliyor, kaynak değişimi hata                                                                                                                                                                                                                                                                                                    |
| F2-07 | **Tamamlandı:** literal arama                                                   | `find_in_document`                                             | Exact/contains, text/attribute ve scope adresi doğru; regex kabul edilmiyor; tam olmayan taramada exact total yok                                                                                                                                                                                                                                                                                                        |
| F2-08 | **Tamamlandı:** tüm MCP yanıtlarını doğrula                                     | Şema, error envelope ve payload kontrolleri                    | Hata dahil limit üstü yanıt yok; ham mutlak yol/stack yok; false/0/boş string kaybolmuyor                                                                                                                                                                                                                                                                                                                                |
| F2-09 | **Tamamlandı:** bağlantı testleri ve agent walkthrough                          | Sentetik fixture corpus ve araç çağrısı kayıtları              | Aşağıdaki kullanım senaryoları doğru, bounded ve dosya değiştirmeden tamamlanıyor                                                                                                                                                                                                                                                                                                                                        |
| F2-10 | **Tamamlandı:** eşzamanlı DOM bellek bütçesi ve yapılandırılabilir cache boyutu | Ölçülmüş şişme katsayısı ve `createXmlMcpServer` seçeneği      | Düz, attribute-yoğun ve derin şekil için kaynak byte'ı başına RSS katsayısı ölçülür ve dokümana girer; `documentCacheSize` **tek** knob olarak açılır, worker kapasitesi `W ≥ 2S−1`'den türetilir ve invaryant testle sabitlenir; default `S=4` değişmez ve en küçük desteklenen hostta güvenli kalır; knob'un yaptırım değil bütçe olduğu belgelenir — K5 gereği `worker.resourceLimits` WASM linear memory'yi bağlamaz |
| F2-11 | **Tamamlandı:** prolog tarayıcısının kalan encoding aileleri                    | UTF-32 ve EBCDIC prolog fixture'ları                           | Bu encoding'lerde DOCTYPE ya tarayıcı tarafından tespit edilir ya da belge parse'tan önce reddedilir; `doc.dtd` backstop'una düşmek kabul edilen sonuç değildir, çünkü M11 gereği o noktada internal entity zaten genişlemiştir                                                                                                                                                                                          |

| F2-12 | **Tamamlandı:** eşzamanlı listeleme bütçesi | `createGate` ve `maxConcurrentListings` seçeneği | Eşzamanlı listeleme sayısı sınırlı; aşımda kuyruk yerine `resource_limit`; varsayılan 4 |

F2-01/02/03, F1'in kapanış kapısı olduğu için erken tamamlandı. Görev kimlikleri değişmedi; kanıtları [F1 kapanış kaydındadır](../xml-f1-kapanis.md). F2-12, F1 kapanışının sahipsiz kalan tek sınırına commit `0e39a57`'nin kuralı gereği verilen kimliktir: sahipsiz bulgu kaybolan bulgudur.

### F2-06 sayfa birleştirme kapısı

Tool sözleşmesindeki preorder kayıtları, `nodeId`/`parentId`/`childIndex` ilişkisiyle tekrar veya kayıp olmadan birleştirilir. Ancestor context kayıtları sayaca katılmaz. Text/element/comment/PI karışımı sayfa sınırında aynı sırada kalır. `maxDepth` nedeniyle atlanan çocuklar açıkça işaretlenir; cursor bu çocukları tamamlamayı vaat etmez. Kabul fixture'ı tek kayıtlık sayfaları, derinlik sınırını ve daha derin ayrı okuma çağrısını birlikte sınar.

## Agent kabul senaryoları

1. `pom.xml` bağımlılık sürümü: describe ile default namespace öğren, adresli okuma ile doğru bağımlılığı göster. Aynı local name başka URI'de olan tuzak alan seçilmemeli.
2. Sentetik UBL benzeri fatura: satır kimliği ve tutarı string olarak getir; baştaki sıfır ve ondalık yazımı değişmemeli. Hukuki/fatura geçerliliği sonucu verilmemeli.
3. `.csproj` ve legacy namespace'li proje: ikisinde de hedef framework alanını bul; namespace'siz varsayım legacy belgeye uygulanmamalı.
4. Mixed-content açıklama: alt elementler arasındaki text sırası ve whitespace korunmalı.
5. Hatalı veya sınır üstü belge: doğru recovery önerisi; sunucu sonraki normal çağrıda kullanılabilir kalmalı.

Her senaryo için beklenen gerçek düğüm ve içerik fixture manifestinde önceden yazılır; agent'ın makul görünen cevabı tek başına geçiş değildir. Üç tool çağrısı bir kullanılabilirlik hedefidir, zorunlu doğruluk ölçütü değildir.

## Kapsam dışı

XPath, regex, streaming, otomatik type inference, schema validation, ZIP/gzip, XML↔JSON dönüşümü, formatlama, diff, yazma ve XSLT bu fazda yoktur. Motorun desteklediği her API tool olarak açılmaz.

## Bitti ölçütü

F2-01–12 kapandı. [Test matrisinin](../test-stratejisi.md) F2 zorunlu aileleri geçti; sayfa bütçesi ve eşzamanlı DOM şişme katsayısı **ölçüldü** ve [kapanış kaydına](../xml-f2-kapanis.md) girdi; benchmark ve güvenlik testi "sonra"ya bırakılmadı.

**Platform kanıtı alındı**: beş hedef × Node 22/24 native matrisi, `pack` denetimi ve temiz tüketici kurulumu geçti; XML F0 on ayakta F2-10 katsayısını `pass` ile ölçtü. F6 kurulum/yayın kapısına hazır.
