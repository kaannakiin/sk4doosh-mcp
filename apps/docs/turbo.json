{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".tanstack/**"]
    },
    "validate": {
      "inputs": ["scripts/check-content.mjs", "src/content/**"],
      "outputs": []
    }
  }
}
