/**
 * Guard: the name lives here and not in the locale files. A translated name has
 * to be re-earned every time the product changes shape, and it drifted once
 * already — the interface still said "Reader" after the product had grown
 * connected servers and a coding agent.
 *
 * Guard: the interface and `clientInfo.name` read this one value. That name
 * reaches every MCP server this product connects to, so two spellings means the
 * operator reading their own logs and the reader reading the header are looking
 * at different products.
 */
export const PRODUCT_NAME = "Reader Console";
