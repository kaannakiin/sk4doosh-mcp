enum AuthClientType {
  web
  ios
  android
  desktop
  other
}

enum AuthChallengePurpose {
  verifyEmail
  verifyPhone
  phoneLogin
  passwordReset
}

enum ToolApprovalMode {
  always_ask
  remember
}

enum GrantTtl {
  day
  week
  never
}

enum IntegrationApprovalMode {
  always_ask
  remember
  auto
}

enum ToolOverrideMode {
  always_ask
  auto
}
