# The API is reached through the dev server's own origin, so the default
# root-relative /api needs no entry here. Set an absolute URL only when the
# API is served from its own host, which also requires cross-site cookies.
# VITE_CHAT_API_URL=https://api.example.com
