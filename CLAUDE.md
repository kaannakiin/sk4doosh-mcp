# sk-mcp

"Swagger for Agents": an MCP layer embedded into existing backends — spec + SDK per language.

## Package Boundaries

`packages/` is grouped by role, not by dependency — a folder tree cannot express a graph without lying, so the arrows live in this table and in each manifest. `cores/` holds shared machinery (nothing there is installable), `servers/` holds the publishable MCP servers (each has a `bin`), `http/` is the HTTP catalog line, `toolchain/` is config only, `lab/` is the evidence harness. Package **names** are flat and independent of the folder, so `--filter` and `workspace:^` never mention a group.

`products/` is a separate product line that shares only the toolchain (`@sk-mcp/oxlint-config`, `@sk-mcp/typescript-config`). Filter by scope, not by directory: `--filter='@chat/*'` and `--filter='!@chat/*'` partition the repo.

| Package                        | Role                                                                                                                                                                                                                      | Rule                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/http/spec`           | Normative spec: English prose + `schemas/*.schema.json`                                                                                                                                                                   | No runtime code; schemas are the single source of truth for all languages                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `packages/http/conformance`    | Pure JSON fixture corpus + `validate.mjs`                                                                                                                                                                                 | SDKs read JSON from paths; no runtime dependencies may be added here                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `packages/http/core`           | TS reference implementation (including composer/template runtime)                                                                                                                                                         | Types for spec concepts are GENERATED via `pnpm gen`; never write them manually                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/http/openapi`        | OpenAPI ingestion: a Swagger 2.0 / OpenAPI 3.0–3.2 document → `EndpointDescriptor[]`, per-endpoint base URL, security model and diagnostics                                                                               | `private: true`. Consumes `@sk-mcp/core` for its types and **performs no I/O**: `fetch`, `node:http`/`net`/`tls` and `node:fs` are lint-banned across `src/`, and an external `$ref` is read only through the injected `DocumentLoader` — without one a document that has any is `external_ref_blocked`. Never throws for document content: every construct it cannot carry is a diagnostic with an RFC 6901 pointer, and `ingestionSeverities` is `satisfies`-typed so a code without a severity does not compile. Bound by the `openapi-ingestion` fixture profile only ([packages/http/spec/openapi-ingestion.md](packages/http/spec/openapi-ingestion.md), [docs/openapi-kaynak-karari.md](docs/openapi-kaynak-karari.md))                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `packages/cores/mcp-core`      | Published source-agnostic machinery for read-only MCP servers: tool typing, `guard`, response budget, error envelope, cursor codec, stdio server                                                                          | The base both `file-core` (file sources) and `db-core` (SQL sources) build on; **not `packages/http/core`**, which is the HTTP catalog reference implementation. Knows nothing about what a source is: a string literal saying "file", "path", "table" or "query" inside `packages/cores/mcp-core/src` is a defect. Redaction is **injected** through `ErrorContext.redact`, never chosen here — the core cannot know what a given source must not disclose. No runtime dependencies; `zod` and the MCP SDK are peers. `guard()` is the only producer of a `GuardedHandler` and `HandlersOf<D>` accepts nothing else, which is what makes the `mcpCoreLimits.maxPayloadBytes` gate unbypassable. `ToolDefinitions` is read-only and `createMcpSourceServer` accepts nothing else; a tool that only adds a new entry to the server's own output (`ownOutput`, `OwnOutputToolDefinition`, `ToolCatalog`) is registered solely through `createMcpOutputServer`, and `file-core`/`db-core` never re-export any of the four — so a server that names one of them cannot declare a writing tool ([docs/cikti-yazan-tool-karari.md](docs/cikti-yazan-tool-karari.md))                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `packages/cores/file-core`     | The file layer over `@sk-mcp/mcp-core`: sandbox path resolution, listing, document cache, format registry, root redaction, stdio CLI                                                                                      | Consumes `@sk-mcp/mcp-core` and re-exports its full surface, so `excel-mcp` and `xml-mcp` name only this package. **Not `packages/http/core`** and never depends on it in either direction. Published (not `private`) so product servers can depend on it at runtime; `zod` and the MCP SDK are peers. Widens `SourceErrorCode` with the file codes, `Vocabulary` with `rootLabel`/`readableLabel`/`tooLargeRecovery`, and binds `redactRoot` into the core's `ErrorContext.redact` seam inside `guard` — so no file server can emit an envelope carrying an absolute path. The grid layer must not leak into it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `packages/cores/db-core`       | The relational layer over `@sk-mcp/mcp-core`, whose full surface it re-exports: connection pool, cancellation rule, value policy, catalogue introspection, the searchable catalogue snapshot and the four read-only tools | Consumes `@sk-mcp/mcp-core`; **never `packages/http/core`**. Names **no driver** — not a dependency, not a peer: the engine arrives as a `Dialect<TConfig>` and the socket as a `DriverAdapter<TConfig>`, lint-enforced across the package. Produces no SQL text either: `TOP`, `OFFSET`/`FETCH`, `LIMIT`, `sys.`, `information_schema`, `nvarchar`, `mssql`, `pg` inside `packages/cores/db-core/src` is a defect, while ANSI relational nouns are allowed. `SqlText` is minted only by `sqlText()`, and agent text reaches it only through `Dialect.readOnlyGuard`'s `allow` arm — the statement guard is advisory, the database principal is the boundary. A cancelled request's connection is held until it settles and quarantined past `cancelSettleMs`; `Promise.race([run(), abort()])` is the defect this rule exists to prevent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `packages/servers/mssql-mcp`   | Standalone, publishable read-only MCP server for Microsoft SQL Server                                                                                                                                                     | Consumes `@sk-mcp/db-core` and **no other `@sk-mcp/*` package** — db-core re-exports the mcp-core surface, so every product server names exactly one core. `src` is three lint-enforced layers over three root entrypoints: `platform/` (the db-core and node boundary), `dialect/` (the only folder that writes SQL), `driver/` (the only folder that names `mssql`); `cli.ts`, `server.ts` and `index.ts` stay at the `src` root as the single composition root. `sqlText`/`quotedIdentifier` are banned outside `dialect/` by `importNames`. `process.env` is read only in `cli.ts`, each variable by name, so `turbo/no-undeclared-env-vars` forces it into `passThroughEnv`. The query deadline is a timer plus an explicit `cancel()`, never `request.timeout` — measured on `mssql@11.0.2` not to interrupt a running statement. Each logical connection is its own `max: 1` driver pool, because db-core owns the pooling and the cancellation rule depends on one call per socket                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `packages/cores/ooxml-core`    | Published shared reader for OOXML (ECMA-376) containers: zip part source, OPC package, relationships, content types, single-pass XML part scanning                                                                        | Consumed by every OOXML server (`excel-mcp`, later `docx-mcp`/`pptx-mcp`); names **no `@sk-mcp/*` package** — the error factory and the part source arrive by injection, so `@e965/xlsx` never enters it. `saxes` and `fflate` are its only runtime dependencies. A `libxml2-wasm`-style DOM would force a worker for no gain: every OOXML read is a forward scan over a known schema, so SAX is the fit and `xml-mcp`'s engine is a different problem                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `packages/servers/excel-mcp`   | Standalone, publishable MCP server that reads local Excel files                                                                                                                                                           | Not a `@sk-mcp/core` consumer: `EndpointDescriptor` is HTTP-only. Consumes `@sk-mcp/file-core` and `@sk-mcp/ooxml-core`. `src` is four entrypoints over five layers, each a lint-enforced folder: `platform/` (the file-core and node boundary), `grid/` (sees a sheet only through `SheetView`), `metadata/` (the SpreadsheetML part readers under `metadata/spreadsheetml/`, and the report shapes over them), `format/` (bytes to `SheetView`), `tools/`. `cli.ts`, `index.ts`, `server.ts` and `regex-worker.ts` stay at the `src` root: the first three are entrypoints the manifest and the tarball guard pin, and `regex-worker.ts` must keep emitting to `dist/regex-worker.js`. Publishable shape (`bin`, `files`, `publishConfig`, `exports.types → dist`) deviates from the internal convention on purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `packages/servers/xml-mcp`     | Standalone, publishable MCP server that reads local XML documents                                                                                                                                                         | Consumes `@sk-mcp/file-core`; never `@sk-mcp/core`, never `packages/lab/xml-lab`. All parsing runs in a worker: a WASM pointer never crosses into the main process and the document store holds only serializable handles. `libxml2-wasm` is an **exact-pinned runtime dependency** (`0.7.2`, no caret); the worker entry must ship as `dist/xml-worker.js` and the `pack` job guards both                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `packages/servers/pdf-mcp`     | Standalone, publishable read-only MCP server that reads local PDF documents                                                                                                                                               | Consumes `@sk-mcp/file-core`; never `@sk-mcp/core`, never `@sk-mcp/ooxml-core` — a PDF is not an OOXML container. `@firecrawl/pdf-inspector` is an **exact-pinned runtime dependency** (`1.23.0`, no caret) and `src/engine/` is the only folder allowed to name it, lint-enforced: the library reports page numbers 0-based in some fields and 1-based in others **within one result object**, so a second import site would reintroduce that bug. Every page number on the MCP surface is 1-based. Page selection is validated against the real page count before the library sees it, because an out-of-range index is answered with a phantom `needsOcr` page and a negative one wraps through u32. `describe_document` never builds its OCR page list from `classifyPdf`, which flags every page of a document containing one scan. The engine ships **no darwin-x64 binary** and its wasm fallback is unpublished, so the CI native matrix skips this package on that leg. **Performs no network access**: `fetch`, `node:http`, `node:net` and their siblings are lint-banned across `src/`, and the ban is folded into every layer's rule because a later oxlint override replaces `no-restricted-imports` instead of merging with it. OCR is _supported_, never _implemented_: `src/ocr/port.ts` declares `PageRasterizer` (page → pixels) and `OcrProvider` (pixels → text), both injected through `PdfMcpServerOptions.ocr`, so this package names no rasterizer and no model client — the same stance `db-core` takes towards a driver. `ocr` defaults to false on every tool; with no provider bound `ocr: true` is `ocr_unavailable`, never a silent fall back to untranscribed pages. An empty transcription leaves the page `needsOcr`, and a provider's answer for a page it was not asked about is discarded ([packages/servers/pdf-mcp/README.md](packages/servers/pdf-mcp/README.md))                                                                                                                 |
| `packages/servers/llm-mcp`     | Standalone, publishable MCP server that lets a planning agent (codex) hand bounded language work to a local model                                                                                                         | Consumes `@sk-mcp/mcp-core` directly and **no other `@sk-mcp/*` package** — the one server that names mcp-core itself, because the own-output tool type (`createMcpOutputServer`) is exported nowhere else ([docs/llm-mcp-plani.md](docs/llm-mcp-plani.md)). `src` is three lint-enforced layers over three root entrypoints: `platform/` (the mcp-core and node boundary), `backend/` (one model host), `tools/` (the MCP surface, which sees a host only through `backend/port.ts`); the concrete backend is bound in `cli.ts`. **Only `src/backend/ollama.ts` reaches the network**, and only through the global `fetch` — the global and `node:http`/`net`/`tls` are lint-banned everywhere else, the ban folded into every layer's rule. `process.env` is read only in `cli.ts`, each `SKMCP_LLM_*` variable by name. Every model call goes through `createSerialBackend`: one GPU was measured serial and codex fires calls in parallel, so the queue is what keeps each call inside its own timeout; `probe` bypasses it. The server counts tokens before it sends, because Ollama drops the head of an oversized prompt without an error: an input over `num_ctx × 0.45` is refused as `input_too_large`, never sent. Every path argument is resolved by `platform/workspace.ts` against the working directory, checked on the requested path and again on its realpath, and only a branded `WorkspacePath` can be read. `local_map` is the one own-output tool, so the catalogue is `satisfies ToolCatalog` and registered through `createMcpOutputServer`. **Only `src/platform/workspace.ts` imports `node:fs`**, lint-enforced like the network ban, and its `createOutput` is the only write: the output directory is re-checked on its realpath, the name is the server's (safe alphabet, fixed `.csv`), and the file is opened `wx`. A long `summarize`/`extract` is split by `tools/chunk.ts` and merged in rounds (at most 32 chunks, 3 rounds); every other kind still refuses an input over the budget |
| `packages/servers/openapi-mcp` | MCP server that exposes an OpenAPI document as the search-first HTTP catalog over a **remote** backend, calling it with `fetch`                                                                                           | `private: true` (it depends on the private `@sk-mcp/core`). Consumes `@sk-mcp/core` and `@sk-mcp/openapi`, never `@sk-mcp/mcp-core`: `createMcpSourceServer` accepts read-only tools only and `invoke_tool` can send DELETE, so every meta-tool answer goes through core's `emitGuarded` and refusal constructors instead — the one sanctioned exception to the hand-built-`CallToolResult` rule. **Only `src/net/fetch.ts` reaches the network**, with a host allowlist (the root server plus `allowHosts`), `redirect: "manual"` and a byte cap applied while the body streams; the global and `node:http`/`net`/`tls` are lint-banned everywhere else. Only `src/platform/files.ts` touches the filesystem, and a referenced file is checked on its realpath against the document's directory. `process.env` is read only in `cli.ts`: `SKMCP_OPENAPI_CONFIG` by name, and the secrets the config names through one lookup. The caller's MCP token is never forwarded to the backend ([docs/uzak-kimlik-karari.md](docs/uzak-kimlik-karari.md), [packages/http/spec/credentials.md](packages/http/spec/credentials.md))                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `packages/lab/xml-lab`         | F0 evidence harness for the XML engine gate: fixture generators, isolated probes, evidence collector                                                                                                                      | `private: true` with **no `build` task**, so no `dist/` and no importable surface. Nothing may depend on it in either direction — `packages/servers/xml-mcp` never imports it. Never added to the `pack` job. `libxml2-wasm` lives here as an **exact-pinned devDependency**; in `xml-mcp` it is a runtime dependency, both pinned to the same `0.7.2`. A caret in either invalidates the F0-01 integrity record. Holds no product code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `sdks/*`                       | Language SDKs (dotnet, nestjs)                                                                                                                                                                                            | Do not put SDKs under `packages/`; the NestJS synthetic context is created by the SDK — `light-my-request` is FORBIDDEN (it poisons Express)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `products/chat/contracts`      | The chat product's contract layer: every zod schema in the product, one topic per file under a domain folder                                                                                                              | `private: true`; `zod` is a **peer** on the 4.x line. Locale-free: it carries schemas and keys, never user-facing prose. No barrel — `exports` publishes leaf subpaths only (`./*` → `src/*.ts` for types, `dist/*.js` at runtime), `sideEffects: false`. Compiled `dist` because `@chat/api` builds with `tsc` and cannot execute a source-only dependency                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `products/chat/api`            | NestJS 12 chat backend, localized (`en`, `tr`)                                                                                                                                                                            | ESM (`type: module`, NodeNext); relative imports are written with `.ts` and rewritten on emit (`rewriteRelativeImportExtensions`), `reflect-metadata` stays the first import of `main.ts`. Built with plain `tsc` — **never `@nestjs/cli`**. Validation is the Nest 12 native surface: `@Body({ schema })` plus the global `ZodValidationPipe`. `nestjs-i18n` is FORBIDDEN (CJS-only against an ESM-only Nest 12); i18n is `i18next` behind an in-repo `I18nModule`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `products/chat/queries`        | The chat product's server-state layer: query keys, `queryOptions`, mutations and the AI SDK chat transport                                                                                                                | `private: true`; `react`, `@tanstack/react-query` and `ai` are **peers** — two Reacts or two QueryClient contexts break identity in silence, the same trap as two zod majors. Reads no `fetch`, no base url and no `import.meta.env`: the platform injects a `ChatClient`, because Metro defines no `import.meta.env` and a native client has no cookie jar. Names `zod` nowhere — schemas arrive structurally as `{ parse }`. No barrel, leaf subpaths only. Tagged `chat`, never `chat-server`, or `@chat/web` cannot depend on it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `products/chat/web`            | TanStack Start + Mantine + Tailwind chat frontend, localized (`en`, `tr`)                                                                                                                                                 | Mirrors `apps/docs` conventions, port 5190. **The locale is not in the url**; it is carried by the `chat_locale` cookie alone, so a conversation's link is the same link in every language. Resolution ranks the explicit choice (the cookie) above `Accept-Language`, and location never enters it — the header says which language the visitor reads, not where they are. With no cookie the client reads back the `lang` the server stamped on `<html>` rather than re-negotiating, because the header is not readable from a browser. Switching writes the cookie and invalidates the router; it never navigates. A module-level i18next singleton is a defect: SSR shares one process, so the instance is created per locale and memoized. Server state is TanStack Query via `@chat/queries`; a bare `fetch` in a component is a defect. `src/routeTree.gen.ts` is generated; never hand-edit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

## Immutable Rules

- **Single source of truth**: Never manually define TS/C# types for spec concepts (`EndpointDescriptor`, `ToolDefinition`, `Fixture`, `Auth`, etc.). Change the schema → run `pnpm turbo run gen` → use the generated types. Never manually modify files under `packages/http/core/src/generated/` or `sdks/dotnet/src/SkMcp.AspNetCore/Generated/`; they are committed to the repository.

- Layering rules are lint rules, and lint fails the build: every package runs `oxlint --deny-warnings`, because a warning nobody reads is not a guard. Each package's `oxlint.config.ts` extends `@sk-mcp/oxlint-config`; an override replaces a rule's options instead of merging them, so a ban that must hold across layers is folded into every override that names the same rule, and `ignorePatterns`/`env` are not inherited through `extends`. A leading underscore marks a binding that exists on purpose without being read; a decorated controller parameter is what the NestJS SDK discovers a route from, so deleting one changes the catalogue.

- `packages/http/core/src/index.ts` may only re-export `export type` from the canonical files. Never re-export the embedded `EndpointDescriptor`/`ToolDefinition` copies from `generated/fixture.ts` (single-definition rule).

- In schemas, `$id` ≡ the file name. The following 2020-12 keywords must not be used: `prefixItems`, `unevaluatedProperties`, `$dynamicRef`, `dependentSchemas` (the code generator does not support them).

- If a schema change breaks fixtures, update the fixtures in the same change.

- **Comments**: No `//` line comments, no TODOs, no progress notes, no JSON comments — explanations go in the spec prose or documentation. Two exceptions, both `/** */` JSDoc/TSDoc:

  1. **Contract** on exported functions, classes, types and interfaces: purpose, `@param`, `@returns`. Factual only. A well-named item needs none.
  2. **Guard**, anywhere it is needed: a comment may state WHY when it protects an invariant, a security property, or a measured trap. It names what the guard is and what breaks without it, and may cite the decision, measurement or test that pins it. Precedents: [limits.ts](packages/servers/xml-mcp/src/host/platform/limits.ts) (`W >= 2S-1`), [doctype.ts](packages/servers/xml-mcp/src/host/chunk/doctype.ts) (the refused encoding families), [text.ts](packages/servers/xml-mcp/src/primitives/text.ts) (the Turkish dotted I), [listing.ts](packages/cores/file-core/src/listing.ts) (RegExp backtracking).

  Design narration is still forbidden: no restating the code, no explaining what a tier "is for", no history.

- The two product lines do not cross. No `apps/*`, `packages/*` or `sdks/*` package may depend on `@chat/*` (`boundaries.tags.chat.dependents.allow: ["chat"]` in `turbo.json`, checked by `pnpm boundaries`), and `@chat/*` source may not import `@sk-mcp/*` product packages (`@sk-mcp/oxlint-config/chat`). The concrete trap is zod: both `@sk-mcp/sdk-nestjs` and `@chat/contracts` declare it as a peer and both are on **4.x**, but nothing in the graph keeps them in step, and two zod majors in one process make `instanceof ZodError` and schema identity fail silently. The boundary is what stops a unilateral major bump on either side from turning into that failure.

- Every zod schema in the chat product lives in `products/chat/contracts`. A `z.` call inside `products/chat/api/src` or `products/chat/web/src` is a defect, environment schemas included (`config/api-env`, `config/web-env`): a schema declared next to one consumer drifts from the other side of the wire within a release. Type-only `zod` imports stay legal so a pipe can name `ZodType`. Enforced by `@sk-mcp/oxlint-config/chat`'s `chatApp` export.

- `@chat/contracts` has no barrel and no bare import. One topic per file under a domain folder (`common/`, `chat/`, `http/`, `config/`), schema and its `z.infer` type in the same file, next to the dependency-free pure functions over that vocabulary (`isLocale`, `negotiateLocale`) so the two sides can never negotiate a locale by two different algorithms. Consumers name the leaf: `@chat/contracts/chat/send-message`, never `@chat/contracts`. The `exports` map has no `"."` entry, so the bare specifier fails at runtime as well as in lint. Adding an `index.ts` would put every domain in `@chat/api`'s module graph, which it pays for unbundled.

- `@chat/api` and `@chat/web` resolve `@chat/contracts` through `exports.default → ./dist/index.js`. Run them through Turbo (`pnpm dev:chat`, `pnpm turbo run test --filter=@chat/api`), never `pnpm --filter @chat/api <task>`: the bare filter skips `dependsOn: ["^build"]` and runs against a stale `contracts/dist`.

- **Language**: `docs/` (internal design records and ADRs) is in Turkish. `packages/http/spec/` and `apps/docs/` are in English — the spec is the documentation site's normative source and has to be readable by the same audience. Everything machine-readable (JSON fields, tool names, code identifiers) is in English everywhere. In `products/chat`, identifiers, JSON fields and i18n **keys** are English too; user-facing copy lives only in `src/i18n/locales/<locale>/*.json` — a UI string literal in a component under `products/chat/web/src` is a defect.

- Always build core through Turbo (`pnpm turbo run build`) — `pnpm --filter @sk-mcp/core run build` skips generation and risks using stale types.

- Silent conflict resolution in tool naming is forbidden: a conflict is an error ([packages/http/spec/naming.md](packages/http/spec/naming.md)).

- `packages/cores/ooxml-core` carries **no format-specific vocabulary**. An `xl/`, `word/` or `ppt/` prefix, a SpreadsheetML/WordprocessingML/PresentationML namespace, or the noun "workbook", "sheet", "document" or "presentation" inside `packages/cores/ooxml-core/src` is a defect. Container nouns — package, part, relationship, archive — are ECMA-376 vocabulary and are allowed. Error instances and recovery advice arrive through the injected `OoxmlErrorFactory`; the core supplies only the factual detail it alone knows.

- `packages/cores/mcp-core` carries **no source-specific vocabulary**. It never names a file, a path, a table or a query; user-facing nouns arrive through the injected `Vocabulary` and whatever must stay secret is removed by the injected `ErrorContext.redact`.

- `packages/cores/file-core` carries **no format-specific vocabulary**. User-facing nouns arrive through the injected `Vocabulary`; error instances arrive through the injected `ErrorFactory`. A string literal saying "workbook", "sheet", "spreadsheet" or "excel" inside `packages/cores/file-core/src` is a defect.

- A published package may never depend on a `private: true` workspace package. `pnpm publish` silently rewrites `workspace:^` to the private package's `0.0.0`, the publish succeeds, and the failure surfaces in the consumer's `install`. The CI `pack` job guards this ([.github/scripts/check-npm-tarballs.py](.github/scripts/check-npm-tarballs.py)).

- No source-server tool response may bypass the payload budget. `json()` and `toToolError()` in `packages/cores/mcp-core` are the only sanctioned response constructors (`file-core` re-exports them and wraps `toToolError` to bind the root redactor); a hand-built `CallToolResult` in a product package is a defect.

- Visibility ≠ enforcement: search filtering is not a security mechanism; enforcement must always happen in the backend pipeline at invoke time.

## Commands

- `pnpm build` / `pnpm lint` (includes validation) / `pnpm check-types` / `pnpm validate`

- TS tests: `pnpm turbo run test --filter=@sk-mcp/core` / `--filter=@sk-mcp/mcp-core` / `--filter=@sk-mcp/db-core` / `--filter=@sk-mcp/mssql-mcp` / `--filter=@sk-mcp/file-core` / `--filter=@sk-mcp/excel-mcp` / `--filter=@sk-mcp/xml-mcp` / `--filter=@sk-mcp/pdf-mcp` / `--filter=@sk-mcp/llm-mcp` / `--filter=@sk-mcp/sdk-nestjs` (Vitest). Run them **through Turbo**, not `pnpm --filter <pkg> test`: that form skips `dependsOn: ["^build"]`, and `excel-mcp` resolves `@sk-mcp/file-core` through `exports.default → ./dist/index.js`, so a bare filter can test against a stale `dist`.

- dotnet side: `pnpm turbo run build --filter=@sk-mcp/sdk-dotnet` (the shim invokes `dotnet build`)

- DemoApi: `dotnet run` — located in `sdks/dotnet/samples/DemoApi`, listens on `http://127.0.0.1:5178`; MCP endpoint `/mcp` (requires a bearer token); demo shortcut `POST /auth/token {"user":"alice"|"bob"|"carol"}`; full OAuth 2.1 flow via the in-repo `samples/DemoAuthServer` mounted at `/oauth` (PRM at `/.well-known/oauth-protected-resource/mcp`)

- agent-client (MCP acceptance harness, drives both the dotnet and the Nest demo): `node sdks/nestjs/samples/agent-client/dist/main.js --scenario smoke|validation-retry|error-envelope`; `SKMCP_AUTH=oauth|token|bearer` (`bearer` reads `SKMCP_TOKEN`, for a real backend), `SKMCP_USER`, `SKMCP_BASE_URL`

- Nest demo: located in `sdks/nestjs/samples/demo-api`; run with `node dist/main.js` (first run `pnpm turbo run build --filter=@sk-mcp/demo-nestjs`); same `/mcp` + `/auth/token` contract

- Docs site: located in `apps/docs` (TanStack Start + Mantine + Tailwind); run with `pnpm --filter @sk-mcp/docs dev`, listens on `http://localhost:5180`; pages are markdown under `src/content/*.md`. This site is sk-mcp's public face, so its content and UI strings are written in **English**, as is `packages/http/spec`, which the site links to as its normative source; the Turkish-prose rule applies to `docs/` only. `apps/docs` carries no i18n layer by design — that decision is scoped to `apps/docs` and does not extend to `products/`.

- Chat product: `pnpm dev:chat` (`turbo run dev dev:types --filter='@chat/*'`) runs `@chat/contracts` in watch mode, `@chat/api` on `http://127.0.0.1:5191` and `@chat/web` on `http://localhost:5190`; `pnpm dev:sk` is the complement. `@chat/api` needs **both** task names: `dev` runs `node --watch dist/main.js` after its own `build` (its `dev` depends on `build`, not only on `^build` — otherwise a clean checkout starts a runner with no `dist`), and `dev:types` is the `tsc --watch` that keeps emitting into it. Turbo supervises both, so neither is backgrounded with `&`. Env comes from `products/chat/{api,web}/.env` through `@nestjs/config` and Vite — Turbo runs in strict env mode and does not load `.env` files itself, so anything passed on the command line has to be declared in that package's `turbo.json` (`env` for build-time values, `passThroughEnv` for runtime ones).

- Package boundary check: `pnpm boundaries` (`turbo boundaries`, experimental — not part of `pnpm lint`).
